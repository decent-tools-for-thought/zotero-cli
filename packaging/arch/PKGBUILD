# Maintainer: Rick
pkgname=zotero-client-sqlite-cli
pkgver=0.1.0
pkgrel=1
pkgdesc='Read-only CLI for querying Zotero client SQLite metadata and PDF annotation positions'
arch=('any')
url='https://www.zotero.org/'
license=('MIT')
depends=('python' 'sqlite')
source=("zotero_sqlite_tool.py")
sha256sums=('SKIP')

package() {
  install -Dm755 "$srcdir/zotero_sqlite_tool.py" "$pkgdir/usr/bin/zotero-sqlite-tool"
}
